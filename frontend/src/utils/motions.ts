import type { Transition } from "framer-motion";

export const desktopModalTransition: Transition = { duration: 0.4, ease: [0.22, 1, 0.36, 1] };
export const mobileModalTransition: Transition = { duration: 0.5, ease: [0.32, 0.72, 0, 1] };
